# Render entrypoint setup

- Added `index.js` Express server with Bot Framework CloudAdapter for Render.
- Configured adapter to read Microsoft app credentials from environment variables.
- Updated `package.json` start script to run `node index.js` and added Express dependency.

